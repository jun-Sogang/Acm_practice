#include <cstdio>
#include <algorithm>
#include <queue>
#include <string>
#include <cstring>
char a[1001];
using namespace std;

int main()
{
    scanf("%s",a);
    int len=a.lenth();
    for(int i=0; i<len; i++)
    {
        q.push(a[i]);
    }
}
